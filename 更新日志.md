## 2026-07-26 10:53 v1.9.5
- **朋友圈海报升级：优先绘制真实微信群二维码 + 优化文案描述**：
  1. **智能二维码渲染引擎**（`js/poster.js`）：绘制 Canvas 朋友圈海报时，若局长上传了微信群/个人二维码图片，海报上直接精准绘制真实二维码图片，彻底解决微信扫描无效或受限于本地域名的问题；未上传时智能回退至真实线上公开 URL。
  2. **海报文案与 UI 优化**（`js/poster.js`, `js/hall.js`）：将海报标题由原“微信朋友圈诱饵海报”全面更名为更得体自然、具有吸引力的“🀄 微信朋友圈组局海报”，提示语同步更新为“保存图片分享至朋友圈/微信群，快速招募同城麻友上车”。
- **版本号升级至 v1.9.5**：更新所有 ES Module 引入版本后缀 (`js/hall.js`, `js/app.js`, `index.html`, `js/poster.js`)。

## 2026-07-26 10:11 v1.9.4
- **发局/改局表单升级：微信群链接改为上传微信群二维码图片**：
  1. **发局/改局弹窗图片上传**（`js/hall.js` - `showCreateRoomModal` / `showEditRoomModal`）：将原“微信群链接（可选）”输入框替换为“微信群二维码图片（可选）”上传控件，结合 HTML5 `FileReader` 将上传图片转为 base64 DataURL 本地存储，支持实时预览与一键删除重选。
  2. **联络卡二维码识别展示区**（`js/hall.js` - `showContactCardModal`）：拼局成功解锁联络卡后，如有上传微信群二维码图片，直接展示卡片化二维码，并附带“长按二维码识别或保存图片入群”操作指引。
- **版本号升级至 v1.9.4**：更新所有 ES Module 引入版本后缀 (`js/hall.js`, `js/app.js`, `index.html`)。

## 2026-07-26 00:52 v1.9.3
- **修复微信群链接输入框无法粘贴**：`showCreateRoomModal` 中群链接输入框 `type="url"` 改为 `type="text"`，解决移动端微信内置浏览器拦截 url 类型输入的问题 (`js/hall.js`)。
- **新增可编辑个人名片**：Header 顶部用户徽章新增 ✏️ 标识并可点击，弹出名片编辑弹窗，支持修改昵称（最多12字）、微信号、手机号，以及从12款预设 DiceBear 机器人头像中选择；保存后徽章实时刷新，下次发局/上车自动带入新信息 (`js/app.js`)。
- **版本号升级至 v1.9.3**：所有 ES Module 引入更新版本查询字符串破除 CDN 缓存 (`js/hall.js`, `js/app.js`, `index.html`)。

## 2026-07-26 00:32 v1.9.2
- **拼局成功后身份确认：暗语码 + 微信群码组合方案**：
  1. **接头暗语码**（`js/store.js`）：新增 `generateSecretCode(roomId, playerIndex)` 纯前端算法。基于"当日日期 + 房间ID + 玩家序号"哈希生成，格式为「颜色+动物 · 4位数」（如 `红虎 · 2847`），每日自动换码、同参数永远稳定输出，零服务器依赖。
  2. **联络卡暗语码展示区**（`js/hall.js` - `showContactCardModal`）：联络卡顶部新增金色暗语码展示卡片，含"复制"按钮，说明文字引导线下见面互报。
  3. **输入暗语确认到场**（`js/hall.js`）：原"麻友到齐/开局"按钮改为"✅ 输入暗语确认到场 (履约+1)"，点击后弹出 prompt 要求输入自己的暗语码，核对正确才触发 `fulfilledCount + 1`，防止随意点击刷信用。
  4. **微信群链接（可选）**（`js/hall.js` - `showCreateRoomModal`）：发局表单末尾新增可选"微信群链接"输入框，局长可粘贴群邀请链接。拼局成功后联络卡内动态展示"💬 点击进入局长的麻友群"绿色跳转按钮（未填写则不显示）。

## 2026-07-26 00:13 v1.9.1
- **清除所有测试数据与占位符**：
  1. `store.js`：`DEFAULT_ROOMS` 改为空数组，新用户默认看到空大厅（引导自主发局），不再显示写死日期的 mock 房间。
  2. `store.js`：移除针对旧测试 ID `room_100_merchant`/`room_101` 的历史强清补丁（已无用）。
  3. `store.js`：移除针对 `fulfilledCount === 8` 的历史测试修正补丁（已无用）。
  4. `store.js`：默认用户 `wechat`/`phone` 由测试占位符 `my_wx_8888`/`13899990000` 改为空字符串，引导用户自行填写真实信息。
  5. `hall.js`：清除所有 `|| 'my_wx_8888'`/`|| '13899990000'` fallback（共 4 处），改为空字符串兜底。
  6. `hall.js`：联络卡微信显示中 `|| 'pangge_888'` 占位改为 `|| '未填写'`，按钮 data-wx 同步清空。

## 2026-07-26 00:03 v1.9.0
- **防一人多占·同时间段独占上车机制 (A+B 组合拳)**：
  1. **方案 A - 独占锁**：同一用户已上某个局后，点击其他局的"我要上车"时，系统先检测冲突，该按钮会直接触发冲突弹窗而非直接加入，杜绝"悄无声息多占" (`js/hall.js`)。
  2. **方案 B - 冲突换车弹窗**：弹窗展示旧局与新局明细，用户确认后系统自动退出旧局（瞬间释放席位给其他麻友）并加入新局，操作顺畅零摩擦 (`js/hall.js - showConflictSwitchModal`)。
  3. **🚪 退车按钮**：已上车的非局长麻友卡片底部增加"🚪 退车"按钮，随时可主动让出席位，不再需要找局长手动踢人 (`js/hall.js`)。
  4. **预设公共牌局**：`store.js` 配置两个其他用户发起的示例房间，方便新用户直接上手体验排期冲突换局功能 (`js/store.js`)。

## 2026-07-25 23:53 v1.8.5
- **彻底解决线上海报二维码空白问题**：
  1. **二维码生成引擎精简重构**：固化标准 Mask 0 掩模阵列，补全 `getLengthInBits` 成员方法，并为 `QRPolynomial.mod` 增加首项 0 剥离保护，彻底杜绝运行期抛出 `TypeError` 导致的画板降级空白问题 (`js/qrcode.js`)。
  2. **破除浏览器与 CDN 旧缓存**：在所有 ES Module 静态引入链接中强制附加版本后缀 `?v=1.8.7`，确保手机微信与 Cloudflare CDN 边缘端 100% 加载最新固化后的二维码算法脚本 (`index.html`, `js/app.js`, `js/hall.js`, `js/poster.js`)。

## 2026-07-25 23:39 v1.8.4
- **修正新用户首登履约次数为 0 次**：
  1. 将 [store.js](file:///d:/9%E3%80%81AI%E7%BC%96%E7%A8%8B/Antigravity%20%E8%B0%B7%E6%AD%8C%E7%BC%96%E7%A8%8B/%E9%BA%BB%E5%B0%86%E5%8C%B9%E9%85%8D/4%E3%80%81H5%E9%BA%BB%E5%8F%8B%EF%BC%8C%E4%BD%A0%E5%9C%A8%E5%93%AA%E9%87%8C/js/store.js) 中新用户首次登录的默认 `fulfilledCount` 由测试预设值 8 修正为 `0`。
  2. 增加缓存自动兼容修补逻辑：如检测到旧 LocalStorage 缓存中的履约次数为原测试残留的 8 次，自动重置修正为 `0` 次，真实记录用户守时履约进度。

## 2026-07-25 23:35 v1.8.3
- **绑定 Cloudflare Pages 官方线上主页**：
  1. 将全量改动成功提交至 GitHub (`main` 分支)，自动触发 Cloudflare Pages 流水线自动部署更新。
  2. 将海报二维码扫描连通地址映射至 Cloudflare Pages 官方域名 `https://mayu-where-ru.pages.dev/` (`js/poster.js`)。

## 2026-07-25 23:15 v1.8.2
- **彻底解决朋友圈海报二维码扫码无效/连通问题**：
  1. **二维码编码容量扩容**：重构 `js/qrcode.js` 中的 RS 纠错码块容量表（Level L，自动扩容至 Version 10），解除 36 字节长度限制（支持最高 271 字节长 URL 编码），彻底解决因长 URL 溢出触发降级导致的二维码缺失/错误。
  2. **本地与线上 URL 智能映射**：自动识别开发与线上环境。在本地开发（`localhost`）模式下，海报自动编码 GitHub Pages 线上真实公开域名（`https://jiaozi008.github.io/MaYu_where-ru/?room=xxx`），确保手机微信/相机扫描海报时能直接连通访问网络页面 (`js/poster.js`)。

## 2026-07-25 23:10 v1.8.1
- **优化发局默认选项与卡片精确公历日期显示**：
  1. **日期卡片显示优化**：重构 `formatTimeRange` 函数，使用正则精准提取年/月/日，卡片呈现为 `今天 (07-25) 19:00-23:00`，彻底修复之前出现的字符串乱码。
  2. **玩法默认选中广东麻将**：发局模态框中的【麻将玩法品类】默认高亮选中 `广东鸡平胡`。
  3. **局名留空自填**：【局名/规则底分】取消原本硬编码填写的“四川血战...”，改为留空加 Placeholder 提示，供用户自定义输入 (`js/hall.js`)。

## 2026-07-25 23:03 v1.8.0
- **升级开局时间为【日期选择 (默认当天) + 时间段 (几点到几点)】**：
  1. 优化发局与编辑模态框：将原单行文本框升级为原生 `type="date"` 日期选择器（默认自动选中今天）以及 `type="time"` 开始时间与结束时间控件。
  2. 上线智能时间格式化函数 `formatTimeRange`：自动识别今天/明天/指定日期，在大厅卡片上生成形如 `今天 19:00-23:30` 或 `明天 14:00-18:00` 的清晰打牌时间段标语 (`js/hall.js`)。

## 2026-07-25 22:58 v1.7.0
- **修复海报响应、上线局长编辑权限与解决 GPS 误报**：
  1. **修复朋友圈海报无响应 Bug**：修复 `js/qrcode.js` 中 `QRCodeImpl` 的形参未定义问题及 Galois Field 多项式模运算除 0 崩溃，海报已成功生成并清晰渲染二维码 (`js/qrcode.js`, `js/poster.js`)。
  2. **上线发起人（局长）信息编辑与解散权限**：
     - 在房间卡片增加【✏️ 编辑本局】与【🗑️ 解散】专属操作按钮。
     - 支持发起人随时修改局名底分、玩法品类、棋牌室详细门牌地址与开局时间 (`js/hall.js`)。
  3. **彻底解决 GPS 电子围栏到场误报**：
     - 增加硬性校验条件：必须在用户主动开启`🎯 GPS精准定位`且目标棋牌室具备真实 GPS 坐标时才判定到场。
     - 在 IP 保底模式下提示 `📍 距目的地约 0.5km (开启 GPS 可触发电子围栏打卡)`，杜绝无脑误提示“您已到达目的地” (`js/geo.js`, `js/hall.js`)。

## 2026-07-25 22:46 v1.6.1
- **全面通用化“全部商圈”下拉菜单选项**：
  1. 彻底移除 `js/geo.js` 中原预置的深圳具体商圈选项（`科技园`、`后海/海岸城`、`西丽`、`车公庙`、`华强北`）。
  2. 升级为全国通用的功能型商圈分类（`全部商圈`、`同城中心`、`CBD核心区`、`大学城/学府区`、`热门商业街`、`近郊/社区`）。
  3. 增加动态采集逻辑：支持根据用户实时 IP/GPS 定位商圈以及大厅已发局房间的真实商圈名称自动动态扩充下拉选项 (`js/geo.js`)。

## 2026-07-25 22:38 v1.6.0
- **修复与优化用户反馈的 6 大体验与功能缺陷**：
  1. **修复“快捷发局”无响应 Bug**：补全 `Geo.getCurrentArea()` 方法，解决发局弹窗内部 `TypeError` 导致的逻辑中断 (`js/geo.js`)。
  2. **重构筛选栏与防赌声明排版布局**：
     - 将大厅筛选栏升级为 2x2 对齐 Grid 网格结构，彻底解决“全部状态”拉长/错位溢出的问题 (`css/style.css`, `js/hall.js`)。
     - 优化 `⚖️ 健康娱乐·严禁赌博` 合规 Banner 标签与文本的对齐样式。
  3. **优化 IP 地理位置识别与多城市支持**：移除原固定的“深圳科技园”强硬编码，集成免 API Key 动态 IP 定位与全国通用商圈适配逻辑 (`js/geo.js`)。
  4. **修复“一键导航”地图调起功能**：升级高德/腾讯/百度地图 Web URI 搜索检索协议，解决导航唤起失败及弹窗拦截问题 (`js/nav.js`)。
  5. **上线真实可扫描的微信朋友圈海报二维码**：封装纯原生 JS QR Code 矩阵生成器 (`js/qrcode.js`)，在 Canvas 海报上实时生成可供微信相机扫码上车的真实二维码 (`js/poster.js`)。
  6. **清空默认测试发布信息**：清空 `store.js` 中的测试 Mock 局数据并加入历史残留数据强清机制，保证全新无杂质的清爽大厅体验 (`js/store.js`)。

## 2026-07-25 22:05 v1.5.0
- **完成 GitHub 远程仓库配置与代码推送到线上**：
  1. 初始化本地 Git 仓库并绑定远程仓库 `https://github.com/Jiaozi008/MaYu_where-ru.git`。
  2. 成功推送 `main` 主分支全量代码到 GitHub，可随时一键部署至 Cloudflare Pages。

## 2026-07-25 20:47 v1.4.5
- **优化电子围栏触发打卡判定作用域**：
  1. 未加入/未上车的局：仅在大厅展示正常物理距离（如：`📍 距目的地约 0.5km`）。
  2. 已成功上车/已加入的同桌局：麻友进入棋牌室方圆 200 米电子围栏内时才触发绿色高亮到场打卡，彻底修正全局泛滥触发的问题。

## 2026-07-25 20:42 v1.4.4
- **上线 GPS 电子围栏 (Geofencing) 自动到场签到算法**：
  1. 在 `js/geo.js` 中集成球面哈夫斯海姆距离计算算法。
  2. 自动检测用户经纬度与目标棋牌室的真实距离。当用户进入方圆 200 米电子围栏内时，系统自动在 H5 界面弹出高亮标签并自动完成“到场电子签到”。

## 2026-07-25 20:39 v1.4.3
- **补充局长忘记点击时的 3 重自动化兜底保底机制**：
  1. 多麻友互确认：同桌 4 名麻友中任意一人到场点击`麻友到齐/开局`，均可触发全员履约+1。
  2. 自动静默结算：开局超时且无爽约投诉时，系统在后台静默自动为同桌全员发放守时履约+1 勋章。

## 2026-07-25 20:38 v1.4.2
- **上线线下到场确认与点名机制**：
  1. 解封联络卡中增加【🎉 人到齐了·开局】按钮，局长一键完成开局签到，全员守时履约次数 +1。
  2. 联络卡中增加【⚠️ 没来放鸽子 (标记封禁7天)】选项，局长可精准标记放鸽者，触发 7 天封禁风控并将席位重新极速开放补位。

## 2026-07-25 20:34 v1.4.1
- **完成 v1.4 深度体验与商业化演进全套功能开发落地**：
  1. 上线【麻将玩法标签与多维度品类筛选】（支持四川血战、广东鸡平胡、红中换三张、长沙麻将等精确检索）。
  2. 上线【高德/腾讯地图一键 Universal URI 导航】（支持在房间与联络卡一键拉起路线规划）。
  3. 上线【2B 棋牌室商户认证与大厅商业化置顶】（认证商家享受紫金全框高亮与最上方优先展示）。
  4. 上线【开局前 30 分钟催发闹钟与极速替换补位】（支持确认到场与局长一键剔除非响应麻友重新放回大厅极速补位）。

## 2026-07-25 20:31 v1.4.0
- **完成 v1.4 深度体验与商业化演进方案设计**：
  1. 玩法标签：四川血战/广东鸡平胡/红中换三张等多玩法标签与筛选。
  2. 地图导航：高德/腾讯地图 Web URI 一键路线规划调起 (`uri.amap.com`)。
  3. 商户认证：`🏪 官方认证棋牌室` 标识、环境实景图与大厅置顶展示。
  4. 开局催发：开局前 30 分钟到场确认与一键替换极速补位。
- 产出设计规范文档 `docs/specs/2026-07-25-v1.4-advanced-features-design.md` 并入档。

## 2026-07-25 18:32 v1.3.1
- **默认容量与自定义分页提升**：默认展示量调整为 10 条/页。
- **动态容量下拉框上线**：分页条增加 `10 条/页`、`20 条/页`、`50 条/页` 下拉选择控件。
- **扩充演示数据源**：在 `js/store.js` 中新增大学城、车公庙、华强北等多商圈、不同时间段的局供分页与筛选展示。

## 2026-07-25 18:29 v1.3.0
- **上线防赌合规提示**：在首页顶部 Banner、上车承诺弹窗及发局弹窗中加入通顺易懂的防赌提示：`⚖️ 健康娱乐，拒绝赌博！本平台仅提供同城麻友搭子匹配，严禁任何形式的金钱赌博行为。`
- **上线多维度联合筛选**：支持商圈 + 开局时间段（白天局/今晚局） + 拼车状态（缺人中/已满车）3 维交叉检索。
- **上线 H5 分页控件**：支持上一页/下一页翻页交互与局数统计。

## 2026-07-25 18:21 v1.2.2
- 补全【拼车成功解封联系卡】完整交互与UI弹窗：
  1. 房间满员或上车成功后，列表卡片按钮自动升级为 `✅ 拼局成功·查看同桌联络卡`。
  2. 新增 `showContactCardModal` 联络卡弹窗，解封同桌 4 名麻友的微信号与电话。
  3. 支持【📋 复制微信】一键写入剪贴板及【📞 电话拨号】直接拉起电话呼叫。

## 2026-07-25 18:13 v1.2.1
- 地理位置定位架构升级：全面落地**“IP 静默保底 + GPS 授权静默升级”**策略。
- 在 `js/geo.js` 中新增 IP 无弹窗识别与 `requestGPSUpgrade` GPS 授权响应。
- 在 `js/hall.js` 大厅顶部加入定位状态面板，支持用户一键手动升至 GPS 高精度定位。

## 2026-07-25 18:00 v1.2.0
- 成功实现《麻友，你在哪儿》H5 同城拼局极简全闭环应用全套核心功能：
  1. 搭建黑金高质感国潮 H5 基础骨架与响应式布局。
  2. 实现【商圈模糊定位 + 手填棋牌室】检索与动态大厅筛选 (`js/geo.js`, `js/hall.js`)。
  3. 实现【防鸽风控与 7 天封禁守时承诺】交互逻辑 (`js/credit.js`)。
  4. 实现【4 格缺人动态拼卡】与 【Canvas 朋友圈国潮扫码海报生成导出】(`js/poster.js`)。
  5. 实现【麻友名片盒 + 局后关系沉淀】与老搭子一键邀约 (`js/contact.js`)。
- 本地 HTTP 服务已成功运行于 `http://localhost:8080`。

## 2026-07-25 17:44 v1.1.0
- 完成《H5麻友，你在哪里》产品方案二次脑暴与优化。
- 确定并批准方案1（极简全闭环增强方案），涵盖 4 大模块：
  1. 地理位置：商圈级模糊定位 + POI + 棋牌室名手填。
  2. 防鸽机制：守时信用分 + 黄金麻友勋章 + 7天爽约封禁。
  3. 社交裂变：微信 4 格动态缺人卡片 + Canvas 扫码海报。
  4. 局后沉淀：麻友名片盒 + 一键邀请老搭子。
- 产出设计规范文档 `docs/specs/2026-07-25-h5-mahjong-optimization-design.md` 并更新上下文记录。

## 2026-07-21 12:48 v0.1.0
- 初始化项目文档结构 (.ai 文件夹)。
- 记录产品核心痛点及“同城雷达”的三种冷启动模式（开放广场、诚意金、担保）。

