# 《麻友，你在哪儿》v1.4 深度体验与商业化演进 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建《麻友，你在哪儿》v1.4 进阶模块，实现麻将多玩法标签与筛选、高德/腾讯一键地图导航调起、2B 棋牌室商家认证与置顶推广、以及开局前 30 分钟催发与极速补位机制。

**Architecture:** 前端继续沿用纯原生 JavaScript (ESM) 模块化架构，在 `js/` 目录下新增 `rules.js`、`nav.js`、`merchant.js`、`alarm.js` 模块，并通过 `js/hall.js` 和 `js/store.js` 完成统一编排。

**Tech Stack:** HTML5, CSS3 Variables, ES6 Modules (ESM), Map Universal Web URI.

## Global Constraints

- 无需复杂构建流或重型第三方依赖。
- 导航链接严格遵循高德/腾讯地图标准 Universal URI（兼容手机 App 拉起与 Web 路线展示）。
- 所有 UI 元素延续黑金国潮暗夜质感。

---

### Task 1: 麻将玩法标签与多维度品类筛选模块

**Files:**
- Create: `js/rules.js`
- Modify: `js/store.js`
- Modify: `js/hall.js`

**Interfaces:**
- Consumes: `Store.getRooms()`
- Produces: `Rules.getAllRuleTypes()`, `Rules.filterRoomsByRule(rooms, ruleType)`

- [ ] **Step 1: 创建玩法标签与规则解析模块 `js/rules.js`**

```javascript
// js/rules.js
export const Rules = {
  ruleTypes: ['全部玩法', '四川血战', '广东鸡平胡', '红中换三张', '长沙麻将', '大众麻将'],

  getAllRuleTypes() {
    return this.ruleTypes;
  },

  filterRoomsByRule(rooms, ruleType) {
    if (!ruleType || ruleType === '全部玩法') return rooms;
    return rooms.filter(room => room.title.includes(ruleType) || (room.ruleTag && room.ruleTag === ruleType));
  }
};
```

- [ ] **Step 2: 在大厅多维度筛选栏中接入“玩法筛选”下拉选框**

修改 `js/hall.js`，在顶部筛选行中加入 `#rule-select`，支持按玩法精准检索牌局。

---

### Task 2: 高德/腾讯地图一键导航集成

**Files:**
- Create: `js/nav.js`
- Modify: `js/hall.js`

**Interfaces:**
- Consumes: `room.address`, `room.district`
- Produces: `Nav.openAmap(address, name)`, `Nav.openTencentMap(address, name)`

- [ ] **Step 1: 创建地图通用 URI 路线调起模块 `js/nav.js`**

```javascript
// js/nav.js
export const Nav = {
  openAmap(address) {
    const encoded = encodeURIComponent(address);
    // 高德地图 Universal URI 标记与导航协议
    const url = `https://uri.amap.com/marker?name=${encoded}&src=mahjong_app`;
    window.open(url, '_blank');
  },

  openTencentMap(address) {
    const encoded = encodeURIComponent(address);
    const url = `https://apis.map.qq.com/uri/v1/search?keyword=${encoded}&referer=mahjong_app`;
    window.open(url, '_blank');
  }
};
```

- [ ] **Step 2: 在“联络卡”与房间卡片中加入 `🗺️ 导航去棋牌室` 按钮**

点击按钮拉起弹出选择：“高德导航” / “腾讯地图”，点击即刻开启路线规划。

---

### Task 3: 2B 棋牌室商户认证与大厅置顶展示

**Files:**
- Create: `js/merchant.js`
- Modify: `js/store.js`
- Modify: `js/hall.js`
- Modify: `css/style.css`

**Interfaces:**
- Consumes: `room.isMerchant`
- Produces: `Merchant.renderBadge()`, `Merchant.sortMerchantRoomsFirst(rooms)`

- [ ] **Step 1: 创建商家认证与服务模块 `js/merchant.js`**

```javascript
// js/merchant.js
export const Merchant = {
  sortMerchantRoomsFirst(rooms) {
    return [...rooms].sort((a, b) => (b.isMerchant ? 1 : 0) - (a.isMerchant ? 1 : 0));
  },

  renderBadge(room) {
    if (!room.isMerchant) return '';
    return `<span class="badge-merchant" style="background:linear-gradient(135deg, #ec4899, #8b5cf6); color:#fff; font-size:0.75rem; padding:2px 6px; border-radius:4px; font-weight:bold;">🏪 官方认证棋牌室</span>`;
  }
};
```

- [ ] **Step 2: 在 CSS 中增加商户黄金边框与置顶标签样式**

修改 `css/style.css` 突出认证商户卡片。

---

### Task 4: 临近开局 30 分钟催发闹钟与极速替换补位

**Files:**
- Create: `js/alarm.js`
- Modify: `js/hall.js`
- Modify: `js/store.js`

**Interfaces:**
- Consumes: `room.startTime`, `room.players`
- Produces: `Alarm.checkNearStart(room)`, `Alarm.replaceFlakePlayer(roomId, playerIndex)`

- [ ] **Step 1: 创建临近开局催发与补位处理模块 `js/alarm.js`**

```javascript
// js/alarm.js
import { Store } from './store.js';

export const Alarm = {
  checkNeedsConfirm(room) {
    // 检查是否开局倒计时，触发确认到场
    return room.status === 'MATCHING' && room.players.length > 1;
  },

  replacePlayer(roomId, playerIndex) {
    const rooms = Store.getRooms();
    const room = rooms.find(r => r.id === roomId);
    if (room && room.players[playerIndex]) {
      const removed = room.players.splice(playerIndex, 1);
      room.status = 'MATCHING';
      Store.saveRooms(rooms);
      alert(`⚠️ 已成功移除非响应麻友【${removed[0].name}】，房间已重新开放，极速在大厅抢人补位！`);
    }
  }
};
```

- [ ] **Step 2: 全流程联动测试与验证**

测试商户置顶、玩法筛选、地图导航与补位操作。
